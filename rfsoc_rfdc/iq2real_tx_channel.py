

class Iq2RealTxChannel:
    def __init__(self, dma, gain, sample_rate, tile_id, block_id):
        # Mode: repeater mode, real time mode

        # Basic info
        self.dac_tile_id = tile_id
        self.dac_block_id = block_id
        self.dac_tile_status
        # DAC status

        #
        self.dma = dma
        self.gain = gain
        self.sample_rate = sample_rate
        self.tile = tile
        self.block = block

    def
